1. run following command on cmd ( windows )</br>
  docker exec testnet2 stellar-core http-command peers | grep address >> ipa.txt
2. open following url on browser</br>
  https://pipeers.streamlit.app/
3. upload "ipa.txt"
